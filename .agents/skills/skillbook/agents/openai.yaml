interface:
  display_name: "Skillbook"
  short_description: "Repo-specific patterns and workflows for skillbook"
  default_prompt: "Use the skillbook repo skill to follow existing architecture, testing, and workflow conventions."
policy:
  allow_implicit_invocation: true